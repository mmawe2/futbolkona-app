'use client'
import {useState} from 'react'; import {supabase} from '@/lib/supabaseClient'; import Nav from '@/components/Nav'
export default function LoginPage(){const [message,setMessage]=useState(''); async function handleLogin(e:React.FormEvent<HTMLFormElement>){e.preventDefault(); const f=new FormData(e.currentTarget); const {error}=await supabase.auth.signInWithPassword({email:String(f.get('email')||''),password:String(f.get('password')||'')}); setMessage(error?'Login failed: '+error.message:'Logged in successfully.')} return <main className="min-h-screen bg-fkblack"><Nav/><section className="mx-auto max-w-md px-6 py-20"><h1 className="text-4xl font-bold">Log In</h1><form onSubmit={handleLogin} className="mt-8 grid gap-4 rounded-2xl border border-white/10 bg-fkcharcoal p-6"><input name="email" type="email" required placeholder="Email" className="rounded-xl bg-black/40 p-3 outline-none"/><input name="password" type="password" required placeholder="Password" className="rounded-xl bg-black/40 p-3 outline-none"/><button className="rounded-xl bg-fkgold px-6 py-3 font-semibold text-black">Log In</button>{message&&<p className="text-sm text-fkmuted">{message}</p>}</form></section></main>}
